Box sx={{ display: 'flex', alignItems: 'center', gap: 0.5 }}>
          <IconButton color="inherit" onClick={toggleRail} sx={{ display: { xs: 'none', md: 'inline-flex' } }} aria-label="Toggle side rail">
            <MenuIcon />
          </IconButton>
          <IconButton color="inherit" onClick={toggleDrawer} sx={{ display: { xs: 'inline-flex', md: 'none' } }} aria-label="Open navigation">
            <MenuIcon />
          </IconButton>
          <IconButton color="inherit" aria-label="Account">
            <AccountCircleIcon />
          </IconButton>
        </Box>
