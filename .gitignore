import * as React from 'react';
import { Backdrop, CircularProgress } from '@mui/material';

export default function BusyOverlay({ open }) {
  return (
    <Backdrop open={!!open} sx={{ zIndex: (t) => t.zIndex.drawer + 10 }}>
      <CircularProgress />
    </Backdrop>
  );
}
