+  // Distinct + stable order (alpha by name, fallback by id)
+  const assoc = React.useMemo(() => {
+    const m = new Map();
+    (associatedRows || []).forEach(r => {
+      const id = r?.csF1SubcategoryID;
+      if (!id || Number(id) === 0) return;
+      if (!m.has(id)) {
+        m.set(id, {
+          id,
+          name: r?.csF1SubcategoryName || `CSF1 #${id}`,
+          lang: r?.csF1SubcategoryLanguage || '',
+        });
+      }
+    });
+    return Array.from(m.values())
+      .sort((a, b) =>
+        (a.name || '').localeCompare(b.name || '') || (Number(a.id) - Number(b.id))
+      );
+  }, [associatedRows]);
