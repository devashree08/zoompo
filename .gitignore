import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  components: {
    MuiInputLabel: {
      styleOverrides: {
        root: {
          top: "45%", // THIS is the key!
          transform: "translateY(-50%) scale(1)", // vertical center
          background: "#fff",
          paddingLeft: 2,
          paddingRight: 2,
          fontSize: 14,
          pointerEvents: "none",
        },
        shrink: {
          top: 0,
          transform: "translateY(-100%) scale(0.85)",
        },
        asterisk: {
          color: "#d32f2f",
          fontWeight: 700,
          fontSize: "1.2em",
        },
      },
    },
  },
});

export default theme;
