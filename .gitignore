// Services
builder.Services.AddScoped<IPortalPolicyService, PortalPolicyService>();

// Turn policies from DB into claims per request
builder.Services.AddTransient<IClaimsTransformation, PortalClaimsTransformation>();
