const patchHeaderForActiveOrg = React.useCallback((subcatID, orgID, patch) => {
  setHeaders(prev =>
    prev.map(h =>
      String(h.csF2SubcategoryID) === String(subcatID) && h.orgID === orgID
        ? { ...h, ...patch }
        : h
    )
  );
}, []);
