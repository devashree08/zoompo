const { data: user, isError } = useQuery({
    queryKey: ['userInfo'],
    queryFn: fetchUserInfo,
    staleTime: 5 * 60 * 1000,
    retry: 1,
  });

  const firstName = isError ? 'NULL' : (user?.firstName ?? '…');
  const lastName  = isError ? 'NULL' : (user?.lastName  ?? '');
