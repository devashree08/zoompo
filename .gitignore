import * as React from 'react';
import { Card, CardContent, Typography, Alert } from '@mui/material';

export default function AddCustomAttributePanel({ orgName, subcatName, editable = false }) {
  return (
    <Card variant="outlined" sx={{ mb: 2 }}>
      <CardContent>
        <Typography variant="subtitle2" sx={{ mb: 1 }}>
          Add Custom Attribute — {orgName || ''} / {subcatName || ''}
        </Typography>
        {editable
          ? <Alert severity="info">Editing to be wired in Phase 4.</Alert>
          : <Alert severity="info">Read-only preview. Editing arrives in Phase 4.</Alert>}
      </CardContent>
    </Card>
  );
}
