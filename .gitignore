const handleCompletionUpdated = React.useCallback((patch) => {
  // patch only the active org + selected subcat header
  patchHeaderForActiveOrg(selectedSubcatID, activeOrgID, patch);
}, [selectedSubcatID, activeOrgID, patchHeaderForActiveOrg]);
