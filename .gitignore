 onChange={(e) => {
    const v = e.target.value || null;
    const patch = v === 'Remove'
      ? { selectionStatus: v, selectionAttestation: null } // <-- clear attestation
      : { selectionStatus: v };
    onSave(row, patch);
  }}


